<h2 class="text-2xl font-bold mb-6"><%= t("account.orders.index_title") %></h2>

<div class="overflow-x-auto">
  <table class="min-w-full border border-gray-200 rounded-lg overflow-hidden">
    <thead class="bg-gray-100">
      <tr>
        <th class="px-4 py-2 text-left text-sm font-semibold text-gray-700"><%= t("account.orders.order_id") %></th>
        <th class="px-4 py-2 text-left text-sm font-semibold text-gray-700"><%= t("account.orders.created_at") %></th>
      </tr>
    </thead>

    <tbody class="divide-y">
      <% @orders.each do |order| %>
        <tr class="hover:bg-gray-50 transition">
          <td class="px-4 py-2">
            <%= link_to order.id,
                account_order_path(order),
                class: "text-blue-600 hover:underline font-medium" %>
          </td>
          <td class="px-4 py-2 text-gray-600">
            <%= l(order.created_at, format: :long) %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
