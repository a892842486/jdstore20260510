<div class="flex justify-between items-center p-4">
  <h1 class="text-2xl font-semibold"><%= t("admin.products.index_title") %></h1>

  <%= link_to t("admin.products.new"),
      new_admin_product_path,
      class: "px-4 py-2 bg-green-500 text-white rounded-lg hover:bg-green-600 transition active:scale-95" %>
</div>
<div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-6 p-4">
  <% @products.each do |product| %>
    <div class="bg-white rounded-lg shadow hover:shadow-lg transition transform hover:-translate-y-1 hover:scale-105">

      <%= link_to product_path(product), class: "block" do %>
      <% if product.image.attached? %>
         <%= image_tag(
              url_for(product.image.variant(resize_to_limit: [300, 300])),
              class: "w-full h-48 object-cover rounded-t-lg"
            ) %>
      <% else %>
        <%= image_tag(
              asset_path("no_image.jpeg"),
              class: "w-full h-48 object-cover rounded-t-lg"
            ) %>
      <% end %>
    <% end %>

      <div class="p-4 text-center">
        <p class="font-semibold text-gray-800 truncate"><%= product.title %></p>
        <p class="text-blue-600 font-bold mt-1"><%= product.price %> <%= t("currency.unit") %></p>
        <div class="mt-3 flex justify-center gap-2">
          <!-- 編輯 -->
          <%= link_to t("admin.products.edit"),
              edit_admin_product_path(product),
              class: "px-3 py-1 text-sm bg-blue-500 text-white rounded hover:bg-blue-600 transition active:scale-95" %>

          <!-- 刪除（業界標準） -->
          <%= button_to t("admin.products.delete"),
              admin_product_path(product),
              method: :delete,
              data: { turbo_confirm: t("admin.products.confirm_delete")},
              class: "px-3 py-1 text-sm bg-red-500 text-white rounded hover:bg-red-600 transition active:scale-95" %>
        </div>
      </div>
    </div>
  <% end %>
</div>
